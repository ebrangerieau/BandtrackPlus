version: '3.8'

services:
  bandtrack:
    build: .
    container_name: bandtrack
    ports:
      - "3000:3000"
    #volumes:
    #  - /volume1/docker/bandtrack/bandtrack.db:/app/bandtrack.db
    environment:
      - HOST=0.0.0.0
      - PORT=3000
    restart: unless-stopped
